<?xml version="1.0" encoding="UTF-8"?>

<project name="build-tkdclub-component" default="zipComponent" basedir=".">
    
    <taskdef resource="net/sf/antcontrib/antcontrib.properties">
        <classpath>
            <pathelement location="/usr/share/java/apache-ant/ant-contrib-1.0b3.jar"/>
        </classpath>
    </taskdef>
    <property name="name" value="tkdclub"/>
    <property name="web.root" value="/home/markus/Dokumente/webroot/dev_tkdclub/"/>
    <property name="src" location="${basedir}"/>
    <property name="adminsrc" location="admin"/>
    <property name="sitesrc" location="site"/>
    <property name="build" location="../zips"/>
    <property name="temp" location="../zips/temp"/>
    
    <property name="sitetest" value="${web.root}components/com_${name}" />
    <property name="admintest" value="${web.root}administrator/components/com_${name}" />
    <property name="manifest" value="${src}/${name}.xml" />

    <xmlproperty file="admin/${name}.xml" keepRoot="false" semanticAttributes="true"/>
    <propertyregex property="version_i"
               input="${version}"
               regexp="\."
               replace=""
               global="true" />
    
    <target name="init">
        <mkdir dir="${build}"/>   <mkdir dir="${temp}"/>
         <copy todir="${temp}">
            <fileset dir="${basedir}"/>
        </copy>
        <move file="${temp}/admin/${name}.xml" todir="${temp}"/>
        <delete file="${temp}/build.xml"/>
    </target>

    <target name="symlink">
        <symlink link="${admintest}" resource="${basedir}/admin" />
        <symlink link="${sitetest}" resource="${basedir}/site" />
    </target>

    <target name="zipComponent" depends="init" description="zips the archive" >
        <zip destfile="${build}/com_${name}_v${version_i}.zip">
            <zipfileset dir="${temp}" excludes="**/.svn/,**/nbproject/**,**/zip/**,*.ant" />
        </zip>
        <delete dir="${temp}"/>
    </target>

    <target name="clean" description="deletes the ${build}-folder">
        <delete dir="${build}" />
    </target>
   
</project>